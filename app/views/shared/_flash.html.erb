<% flash.each do |key, value| %>

  <% return unless value.present? %>

  <div class="alert alert-info alert-dismissible">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <%= content_tag :div, value, class: "text-center" %>
  </div>

<% end %>
